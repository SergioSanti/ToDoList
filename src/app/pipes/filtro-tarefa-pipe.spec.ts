import { FiltroTarefaPipe } from './filtro-tarefa-pipe';

describe('FiltroTarefaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTarefaPipe();
    expect(pipe).toBeTruthy();
  });
});
